<section id="inicio" class="hero">
    <div class="hero-content container">
        <span class="badge">Desde el corazón de Quito</span>
        <h1>Sazón que abraza,<br />tradición que alimenta.</h1>
        <p>
            Disfruta de la auténtica cocina ecuatoriana, preparada con el amor y
            el secreto de la casa. El verdadero sabor de nuestra tierra te
            espera en cada plato.
        </p>
        <div class="hero-btns">
            <button class="btn btn-badge btn-badge-primary btn-open-modal"
                >Ver Menú de la Casa</button
            >
            <a
                href="https://chat.whatsapp.com/BV0eiC6HClm6UeEPX5Fkq7"
                target="_blank"
                rel="noopener noreferrer"
                class="btn btn-badge btn-badge-secondary"
                >Unirse al Grupo WhatsApp</a
            >
        </div>
    </div>
    <div class="hero-image-overlay"></div>
    <div
        class="scroll-indicator"
        onclick="document.getElementById('herencia').scrollIntoView({ behavior: 'smooth' });"
        style="cursor: pointer;"
    >
        <span>Desliza para descubrir</span>
        <i data-lucide="chevron-down"></i>
    </div>
</section>

<style>
    .hero {
        min-height: 100vh;
        background: url("/assets/hero.png") no-repeat center center/cover;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        position: relative;
        color: var(--white);
        margin-top: 0;
        padding-top: 100px;
        padding-bottom: 2rem;
        animation: fadeIn 1s ease;
    }

    .hero-image-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            to right,
            rgba(0, 0, 0, 0.7),
            rgba(0, 0, 0, 0.3)
        );
        z-index: 1;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        max-width: 700px;
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        padding-left: 1.5rem;
        padding-right: 1.5rem;
    }

    .badge {
        background: var(--accent);
        padding: 0.4rem 1rem;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 0.75rem;
        display: inline-block;
        animation: pulse 2s ease-in-out infinite;
    }

    .hero h1 {
        font-size: 4rem;
        line-height: 1.1;
        margin-bottom: 1.5rem;
        text-align: left;
    }

    .hero p {
        font-size: 1.2rem;
        margin-bottom: 2rem;
        opacity: 0.9;
        text-align: left;
    }

    /* Contenedor base de botones */
    .hero-btns {
        display: flex;
        gap: 1rem;
    }

    .scroll-indicator {
        position: relative;
        bottom: auto;
        left: auto;
        width: 100%;
        z-index: 2;
        text-align: center;
        color: var(--white);
        animation: fadeInUp 1.5s ease 1s both;
        margin-top: 2rem;
    }

    .scroll-indicator span {
        display: block;
        font-size: 0.85rem;
        margin-bottom: 0.5rem;
        opacity: 0.9;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .scroll-indicator i {
        display: block;
        margin: 0 auto;
        animation: bounce 2s ease-in-out infinite;
    }

    /* MEDIA QUERIES */
    @media (max-width: 768px) {
@media (max-width: 768px) {
    /* 1. Ajustamos el contenedor para que no se estire tanto */
    .hero-content {
        flex: 0 1 auto; /* Esto le dice: "no te estires, ocupa solo lo necesario" */
        padding-top: 2rem; 
        padding-bottom: 2rem;
        justify-content: flex-start; /* Alinea los elementos hacia arriba en lugar de centrarlos brutalmente */
    }

    /* 2. Reducimos márgenes internos de los textos para compactar */
    .hero h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem; /* Reducimos el margen inferior del título */
    }

    .hero p {
        margin-bottom: 1.5rem; /* Reducimos el espacio después del párrafo */
    }

    /* 3. Ajuste de altura del hero para que no fuerce 100vh si el contenido es corto */
    .hero {
        min-height: auto;
        padding-top: 80px;
        padding-bottom: 40px;
    }
        /* En móvil: apilamos, centramos y ajustamos al ancho del texto más largo */
        .hero-btns {
            flex-direction: column;
            width: fit-content; /* El contenedor mide lo que mide el botón más ancho */
            margin: 0 auto; /* Centra el bloque en la pantalla */
        }

        .hero-btns .btn {
            width: 100%; /* Los botones llenan el ancho del contenedor */
        }
    }

    @media (min-width: 769px) {
        /* En escritorio: comportamiento original, ancho según contenido */
        .hero-btns {
            justify-content: flex-start;
            width: auto;
        }

        .hero-btns .btn {
            width: auto; /* Tamaño natural basado en texto */
        }
    }
</style>
